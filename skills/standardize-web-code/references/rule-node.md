# Node.js 规则

- 分离进程入口、应用用例、领域逻辑与基础设施。入口显式编排监听、连接、信号、优雅退出、未处理错误和日志；被导入模块不得隐式启动服务或建立全局副作用。
- Promise 必须被 `await`、返回或明确处理；后台任务要有错误、重试/退出和可观测策略。用 `try/finally` 或生命周期管理释放连接、文件、流、定时器和订阅。
- 在启动期集中加载与校验配置。保持 ESM/CJS 边界一致，避免混用导入语义、隐式全局与依赖运行顺序。
- 让 CLI、Worker、HTTP 服务的退出码、取消和资源清理符合各自运行环境，不把浏览器规则迁移到 Node。
